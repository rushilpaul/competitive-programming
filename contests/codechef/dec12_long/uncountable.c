#include <stdio.h>#define P 1000000007typedef long long LL;int hcf(int a, int b){ return !a || !b ? a+b : hcf(b,a%b); }int main(){	int t,a,b; scanf("%d\n",&t);	while(t--)	{		int n,m,a,b; scanf("%d %d %d %d",&n,&m,&a,&b);		int D[m], div[m];		for(a=0;a<m;a++) { D[a]=n+a+1; div[a]=a+1; }		for(a=1;a<m;a++)			for(b=0;b<m;b++)			{				int h = hcf(div[a],D[b]);				div[a] /= h;				D[b] /= h;				if(div[a] == 1) break;			}		LL p=1;		for(a=0;a<m;a++)			p = (p*D[a])%P;		printf("%lld\n",p);	}	return 0;}