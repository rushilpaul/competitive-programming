#include <stdio.h>void quicksort(int *x,int first,int last){    int pivot,j,temp,i;     if(first<last)	 {         pivot=first;         i=first;         j=last;         while(i<j)		 {             while(x[i]<=x[pivot]&&i<last)                 i++;             while(x[j]>x[pivot])                 j--;             if(i<j){                 temp=x[i];                  x[i]=x[j];                  x[j]=temp;             }         }         temp=x[pivot];         x[pivot]=x[j];         x[j]=temp;         quicksort(x,first,j-1);         quicksort(x,j+1,last);    }}int main(){	int t; scanf("%d",&t);	while(t--)	{		int a,n,k; scanf("%d",&n);		int ar[n];		for(a=0;a<n;a++) scanf("%d",&ar[a]);		scanf("%d",&k);		int num = ar[k-1];		quicksort(ar,0,n-1);		for(a=0;a<n;a++) if(ar[a] == num) printf("%d\n",a+1);	}	return 0;}